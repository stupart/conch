# Conch plugin template

`conch install-plugin` materializes this shared Claude Code/Codex template into the stable plugin distribution directory, refreshes its instruction prose, and adds the generated absolute-path `.mcp.json`.
