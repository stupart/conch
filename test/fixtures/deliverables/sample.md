# Weekly recap — deliverable check

A markdown deliverable must render **as a document**, not as raw syntax.

## What changed
- The router learned `pdf`, `markdown`, and `text`
- Tall images fit the width and scroll
- `inline code` stays monospace

## Numbers
| surface | before | after |
| --- | --- | --- |
| markdown | raw syntax | rendered |
| text | white page | dark, monospace |

1. ordered
2. items
   - with a nested bullet

> a quote carries an indent

```
let code = "keeps its block"
```
